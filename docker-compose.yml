services:
  backend:
    build: ./backend
    volumes:
      - db-data:/app/data
    environment:
      - DB_PATH=/app/data/banking.db

  frontend:
    build: .
    ports:
      - "3000:80"
    depends_on:
      - backend

volumes:
  db-data:
